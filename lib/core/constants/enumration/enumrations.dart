//enum for payment method
enum PaymentMethod {
  momoPayment,
  payStact,
  payPal,
  masterCard,
  visaCard,
}

//....enums for forecasting
enum ForecastingFilter {
  weekly,
  monthly,
  daily,
}

//....enums for stats
enum StatsEnumuration {
  currentStats,
  historicStats,
}
